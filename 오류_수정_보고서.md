# Gemini API 오류 수정 보고서

## 프로젝트
- **앱**: 중1 에세이 숙제 도우미 (`app.py`)
- **목표**: Gemini API 연동

---

## 오류 1 — 모델명 오류 (404 NOT_FOUND)

### 오류 메시지
```
404 NOT_FOUND: models/gemini-2.0-flash-preview is not found for API version v1beta
```

### 원인
존재하지 않는 모델명 사용. `gemini-2.0-flash-preview`는 Google API에 등록되지 않은 모델명.

### 시도한 모델명 목록
| 모델명 | 결과 |
|---|---|
| `gemini-2.0-flash-preview` | 404 NOT_FOUND |
| `gemini-2.0-flash-001` | 404 NOT_FOUND |
| `gemini-1.5-flash` | 404 NOT_FOUND (v1beta 미지원) |
| `gemini-2.0-flash-lite` | 미확인 |
| `gemini-3-flash-preview` | **정상 작동** |

### 수정
```python
# 수정 전
model="gemini-2.0-flash-preview"

# 수정 후
model="gemini-3-flash-preview"
```

---

## 오류 2 — 무료 할당량 초과 (429 RESOURCE_EXHAUSTED)

### 오류 메시지
```
429 RESOURCE_EXHAUSTED: Quota exceeded for metric:
generativelanguage.googleapis.com/generate_content_free_tier_requests,
limit: 0, model: gemini-2.0-flash
```

### 원인
`gemini-2.0-flash` 계열 모델은 **무료 티어(Free Tier)에서 한도가 0**으로 설정되어 있어 호출 자체가 불가능.

### 수정
`gemini-3-flash-preview` 모델로 변경하여 해결.

---

## 오류 3 — SDK 버전 혼용 문제

### 원인
두 가지 Gemini Python SDK가 존재하며 API 버전과 지원 모델이 다름.

| 구분 | 구버전 SDK | 신버전 SDK |
|---|---|---|
| 패키지명 | `google-generativeai` | `google-genai` |
| import | `import google.generativeai as genai` | `from google import genai` |
| API 버전 | v1 | **v1beta** |
| 클라이언트 | `genai.GenerativeModel(...)` | `genai.Client(api_key=...)` |
| `gemini-1.5-flash` | 사용 가능 | **404 오류** |
| `gemini-2.0-flash` | 사용 가능 | **할당량 0** |

구버전 SDK로 교체를 시도했으나, `gemini-1.5-flash`도 v1beta 오류 발생.
→ 신버전 SDK + `gemini-3-flash-preview` 조합으로 최종 해결.

---

## 최종 적용 코드

```python
from google import genai  # 신버전 SDK

def call_gemini(prompt_text: str) -> str:
    client = genai.Client(api_key=st.session_state['api_key'])
    response = client.models.generate_content(
        model="gemini-3-flash-preview",
        contents=prompt_text
    )
    return response.text
```

---

## 결론

| 항목 | 최종 선택 |
|---|---|
| SDK | `google-genai` (신버전) |
| 클라이언트 생성 | `genai.Client(api_key=...)` |
| 모델 | `gemini-3-flash-preview` |
| 참조 앱 | 유튜브 영어 학습 앱과 동일한 패턴 적용 |
